# Agent-Orchestrator settings
# Upstream model identifier served by Ollama/OpenAI-compatible backend.
ORCHESTRATOR_MODEL=phi3
# Name exposed to clients (AnythingLLM/OpenWebUI). Must match the model field they request.
ORCHESTRATOR_API_MODEL=go-agent-1
ORCHESTRATOR_BASE_URL=http://ollama:11434/v1
ORCHESTRATOR_API_KEY=ollama
ORCHESTRATOR_PORT=8080
ORCHESTRATOR_INSTANCE=orchestrator-1
ORCHESTRATOR_DESCRIPTION=Primary orchestration agent for AnythingLLM.

# Child agent settings
# Backend model that the child agent wraps.
CHILD_AGENT_MODEL=llava-child
# Display name that the orchestrator will surface to clients.
CHILD_AGENT_API_MODEL=child-agent
CHILD_AGENT_BASE_URL=http://ollama:11434/v1
CHILD_AGENT_API_KEY=ollama
CHILD_AGENT_PORT=8081
CHILD_AGENT_INSTANCE=child-agent
# Verbose description that explains the child agent’s specialty to planners.
CHILD_AGENT_DESCRIPTION=Child agent configured for llava-child vision reasoning.

# MCP HTTP tools settings
HTTP_TOOLS_PORT=8082
HTTP_TOOLS_INSTANCE=http-tools
